export interface TeamMember {
  name: string;
  role: string;
  image: string;
  department?: string;
}

export interface Advisor {
  name: string;
  role: string;
  image: string;
  description: string;
}
